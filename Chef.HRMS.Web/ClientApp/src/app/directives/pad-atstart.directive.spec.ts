import { PadAtstartDirective } from './pad-atstart.directive';

describe('PadAtstartDirective', () => {
  it('should create an instance', () => {
    const directive = new PadAtstartDirective();
    expect(directive).toBeTruthy();
  });
});
